#### 登录
## 后端接口:
1. 登录接口, method: POST, url: 'cms/app'
2. 验证接口, method: POST, url: 'user/verify-token'
3. 登出接口, method: POST, url: 'user/logout'

### 用户成功登录流程
## 前端
1.携带 用户名 和 密码 请求登录接口

4.接收到 token 并保存在浏览器 localStorage

## 后端
2. 去 mysql 查询 账号密码是否正确，如果正确前往 3. , 错误的话 直接返回
3. 查询用户角色信息, 生成 token = UUID(用户id + 当前时间戳 + ...), 将 {token: 角色信息} 保存到 redis, 并且设置一个合适的过期时间, 将 token 返回给前端

### 登录验证 
## 前端
1. (路由守卫: 所有路由跳转前 排除登录/404白名单 或者 请求拦截器: 除登录和操作记录接口外,  所有接口请求前) 在请求头中携带 token 请求验证接口
3-1. 说明 token 没有过期 前端不做任何处理
3-2. 说明 token 已过期, 删除 localstorage 的 token 然后跳转 登录页面 要求重新登录
## 后端
2. 从 redis 获取对应 token 的缓存, 如果存在 则刷新 redis token 的过期时间并返回 true 跳转3-1. 否则返回 false 跳转3-2.

### 用户登出
## 前端
1. 请求头携带 token 请求登出接口
3. 删除 localStorage 中的 token 跳转登录页面
## 后端
2. redis 删除对应 token 的缓存, 返回登出成功

#### 角色
### 接口
1. 获取角色信息, method: GET, url: 'user/get-role-info'
### 获取角色
## 前端
1. 请求头携带 token 请求 获取角色信息 接口 
3. 根据角色信息, 展示一级目录. 比如: 超管能看到全部一级目录, 图像组的用户仅能看到图像生成目录
## 后端
2. 通过 token 查询到 角色信息(用户id, 角色名称, 对应权限的一级目录 ...), 返回 角色信息

#### 操作记录
### 接口
1. 记录操作接口, method: POST, url: cms/system-operate-log

## 前端
1. 通过 请求响应器 对白名单(登录、验签、登出、记录操作接口...)之外的接口 在请求成功之后都 携带用户操作 请求记录操作接口
## 后端
2. 将 用户id + 操作类型 + 具体数据 等信息写入 mysql